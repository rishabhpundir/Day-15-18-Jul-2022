from functools import reduce

# REDUCE() USING LAMBDA FUNCTION

# num_list = [1, 3, 5, 7, 2, 10]

# add = (reduce(lambda x, y: x+y, num_list))
# print(add)

# product = (reduce(lambda x, y: x*y, num_list))
# print(product)